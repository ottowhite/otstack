## Codebase Patterns
- `PyGitHubRepository` uses GitPython's `self._git_repo.heads` to get branches (not `.references` which includes tags/stash)
- Test helpers: `_make_repo()` for MockRepository, `_make_pygithub_repo()` for PyGitHubRepository with real git repos
- Line length limit is 88 characters (ruff enforced)
- Pre-commit hooks run lint, typecheck, and tests automatically
- Mock classes live in `test/otstack/helpers/` (MockBranch, MockRepository, MockPullRequest, etc.)
- `TrackingCommandRunner` in test helpers tracks commands for verification
- GitPython's `repo.remote().refs` gives remote refs; use `ref.remote_head` to get branch name (excludes "HEAD" manually)
- `_make_repo()` in test_below.py and test_above.py accepts `remote_branches` parameter for MockRepository
- MockRepository has `_remote_branches: Sequence[str]` field for remote branch testing
- Non-dry-run `below()`/`above()` tests need `working_dir` in MockRepository (for git fetch step)
- `SimpleBranch` can represent remote refs like `origin/main` for `create_branch`
- `questionary.text()` accepts a `default` parameter to pre-fill the input; user can accept with Enter or type to override
- Auto-default worktree: in main.py, compute `Path(local_path).resolve().parent / branch` before the interactive check so branch+title skips interactive mode

## 2026-03-07 - US-001
- What was implemented: Fixed `get_branches()` in `PyGitHubRepository` to use `self._git_repo.heads` instead of `self._git_repo.references`, so tags, stash refs, and other non-branch refs are excluded
- Files changed:
  - `src/otstack/PyGitHubRepository.py` - Changed iteration from `references` to `heads`
  - `test/otstack/test_Repository.py` - Added `TestPyGitHubRepositoryGetBranches` with 4 tests: empty when no git repo, returns only actual branches, tag does not appear as branch, stash ref does not appear as branch
- **Learnings for future iterations:**
  - GitPython's `repo.references` includes ALL refs (branches, tags, stash, notes, etc.) - always use `repo.heads` for branches only
  - The `hasattr(ref, "name")` check in the old code was unnecessary since all refs have names
  - Tests using real git repos (via `tmp_path`) are in `test_Repository.py`, while mock-based tests are in `test_below.py`/`test_above.py`
---

## 2026-03-07 - US-002
- What was implemented: Added `get_remote_branches()` to check remote branch existence in below/above validation
- Files changed:
  - `src/otstack/Repository.py` - Added `get_remote_branches()` to protocol
  - `src/otstack/PyGitHubRepository.py` - Implemented `get_remote_branches()` using `self._git_repo.remote().refs`
  - `src/otstack/OtStackClient.py` - Added remote branch check in both `below()` and `above()` validation
  - `test/otstack/helpers/MockRepository.py` - Added `_remote_branches` field and `get_remote_branches()` method
  - `test/otstack/test_below.py` - Added 3 tests: remote-only exists, both exist, neither exists
  - `test/otstack/test_above.py` - Added 3 tests: remote-only exists, both exist, neither exists
  - `test/otstack/test_Repository.py` - Added `TestPyGitHubRepositoryGetRemoteBranches` with 3 tests using real cloned repos
- **Learnings for future iterations:**
  - To test remote branches with real git, create a bare repo and clone it - then push branches to get remote refs
  - `ref.remote_head` gives the branch name without the `origin/` prefix
  - The "HEAD" symbolic ref in remote refs must be filtered out explicitly
---

## 2026-03-07 - US-003
- What was implemented: Added `git fetch origin <destination>` before creating branch in `below()`, and updated branch creation to use `origin/<destination>` instead of the local ref
- Files changed:
  - `src/otstack/OtStackClient.py` - Added fetch command before `create_branch`, changed from_branch to `SimpleBranch(name=f"origin/{dest}")`, imported `SimpleBranch`
  - `src/otstack/BelowDryRunResult.py` - Added fetch step as step 1 in `format_output()`, renumbered subsequent steps (now starts at step 4 instead of 3)
  - `test/otstack/test_below.py` - Added `test_fetches_remote_before_creating_branch` and `test_creates_new_branch_from_origin_destination`, added `working_dir="/tmp/repo"` to all non-dry-run tests, updated dry-run format test to check for fetch step
  - `test/otstack/test_main.py` - Added `_working_dir="/tmp/repo"` to `_make_repo_with_pr()` fixture
- **Learnings for future iterations:**
  - `below()` now calls `repo.get_working_dir()` before the mutation phase, so all non-dry-run tests must provide `working_dir` to MockRepository
  - `SimpleBranch` can be used as a ref for `create_branch` - the implementation just uses `from_branch.name` in `git branch <name> <ref>`, so `origin/main` works fine
  - When adding new steps that require repo info (like working_dir), check all test helpers and test_main.py fixtures too
---

## 2026-03-07 - US-004
- What was implemented: Updated `InteractivePrompter.prompt_below_above_inputs()` to accept optional pre-filled `branch`, `title`, and `worktree` parameters; only prompts for values that are `None`. Updated `main.py` to pass already-provided CLI args to the prompter.
- Files changed:
  - `src/otstack/InteractivePrompter.py` - Added `branch`, `title`, `worktree` optional params; skip prompts when values are pre-filled
  - `src/otstack/main.py` - Pass `args.branch`, `args.title`, `args.worktree` to `prompt_below_above_inputs()`
  - `test/otstack/test_InteractivePrompter.py` - New test file with 6 tests covering: each single arg provided, two-of-three combinations, and no args provided
- **Learnings for future iterations:**
  - Use `_MOD = "otstack.InteractivePrompter.questionary"` constant to keep patch paths under 88 chars
  - Mock questionary by creating a `MockQuestion` class with `.ask()` method, and use `side_effect` on `questionary.text`/`questionary.confirm` to return them
  - Track which prompts were called via a `calls: list[str]` that captures prompt text in the `side_effect` function
---

## 2026-03-07 - US-005
- What was implemented: Auto-default worktree path from branch name. When `--worktree` is omitted and `--branch` is provided, worktree defaults to `../<branch-name>` (resolved as an absolute path relative to the repo working directory). Interactive mode shows the default in the worktree prompt via `questionary.text(default=...)`.
- Files changed:
  - `src/otstack/main.py` - Added `from pathlib import Path` import, auto-default worktree before interactive check
  - `src/otstack/InteractivePrompter.py` - Added `default=f"../{branch}"` to worktree `questionary.text()` call
  - `test/otstack/test_InteractivePrompter.py` - Added `TestWorktreeDefaultFromBranch` class with 4 tests: default from pre-filled branch, default from prompted branch, override default, pre-filled worktree skips default
  - `test/otstack/test_main.py` - Added 2 tests: auto-default worktree from branch, explicit worktree overrides default
- **Learnings for future iterations:**
  - `questionary.text(default=value)` pre-fills the input so user can accept with Enter
  - To track kwargs passed to mocked `questionary.text()`, use `defaults: dict[str, str]` and capture `kwargs["default"]` in the side_effect
  - Can't mock `otstack.main.os.path.exists` because main.py doesn't import `os` directly - the `os.path.exists` check is in OtStackClient
- Interactive mode `questionary.confirm()` calls: draft prompt comes before direnv/copy/dry-run prompts. Order matters for test mocks using `side_effect`.
- `TrackingCommandRunner` has `raise_called_process_error_for` field to simulate `subprocess.CalledProcessError` for specific subcommands (matches any element in `command` list)
---

## 2026-03-07 - US-006
- What was implemented: Added `draft: bool = False` parameter to `Repository.create_pr` protocol, `PyGitHubRepository.create_pr`, and `MockRepository.create_pr`. PyGitHubRepository passes `draft=` to `self._gh_repo.create_pull()`.
- Files changed:
  - `src/otstack/Repository.py` - Added `draft: bool = False` parameter to `create_pr` protocol
  - `src/otstack/PyGitHubRepository.py` - Added `draft` parameter and passes it to `create_pull()`
  - `test/otstack/helpers/MockRepository.py` - Added `draft` parameter, updated `created_prs` type to 4-tuple
  - `test/otstack/test_Repository.py` - Added `TestPyGitHubRepositoryCreatePrDraft` with 2 tests using MagicMock for `_gh_repo`
  - `test/otstack/test_below.py` - Updated tuple unpacking for `created_prs` 4-tuple
  - `test/otstack/test_above.py` - Updated tuple unpacking for `created_prs` 4-tuple
- **Learnings for future iterations:**
  - To test `PyGitHubRepository.create_pr` without a real GitHub connection, use `MagicMock()` for `_gh_repo` and set it after construction: `repo._gh_repo = gh_repo`
  - PyGithub's `Repository.create_pull()` accepts a `draft` parameter natively
  - When changing `created_prs` tuple size in MockRepository, update all test files that unpack the tuple
---

## 2026-03-07 - US-007
- What was implemented: Added `--draft` / `-d` flag to both `below` and `above` CLI commands. The flag flows through `main.py` -> `OtStackClient.below()`/`above()` -> `repo.create_pr(draft=...)`. Interactive mode asks "Create as draft PR?" with default yes. Dry-run output shows "(draft)" next to PR creation step when applicable.
- Files changed:
  - `src/otstack/main.py` - Added `--draft`/`-d` argparse flag for both commands, pass `draft=args.draft` to client calls, propagate from interactive inputs
  - `src/otstack/OtStackClient.py` - Added `draft` parameter to `below()` and `above()`, passed to dry run results and `repo.create_pr()`
  - `src/otstack/BelowDryRunResult.py` - Added `draft: bool = False` field, `format_output()` shows "(draft)" label
  - `src/otstack/AboveDryRunResult.py` - Added `draft: bool = False` field, `format_output()` shows "(draft)" label
  - `src/otstack/InteractivePrompter.py` - Added `draft` field to `BelowAboveInputs`, added "Create as draft PR?" confirm prompt (default=True)
  - `test/otstack/test_below.py` - Added 4 tests: draft passed to create_pr, draft defaults false, dry-run shows draft label, dry-run omits label when not draft
  - `test/otstack/test_above.py` - Added 4 tests: draft passed to create_pr, draft defaults false, dry-run shows draft label, dry-run omits label when not draft
  - `test/otstack/test_InteractivePrompter.py` - Added 2 tests: draft prompt asked with default=True, draft=False when declined
  - `CLAUDE.md` - Added `--draft, -d` to optional args for both commands
- **Learnings for future iterations:**
  - Adding a new `questionary.confirm()` prompt changes the order of calls in test mocks that use `side_effect` - ensure existing tests' `fake_confirm` handles the additional call
  - `draft: bool = False` as a dataclass default means it's backwards-compatible with existing callers
---

## 2026-03-07 - US-008
- What was implemented: Catch `subprocess.CalledProcessError` from git push in `below()` and `above()`, re-raise as `ValueError` with clear message including branch name, connectivity/auth suggestion, and list of already-created artifacts (branch, worktree).
- Files changed:
  - `src/otstack/OtStackClient.py` - Wrapped push calls in try/except for `subprocess.CalledProcessError`, raise `ValueError` with descriptive message
  - `test/otstack/helpers/TrackingCommandRunner.py` - Added `raise_called_process_error_for` field to simulate `CalledProcessError` for specific subcommands
  - `test/otstack/test_below.py` - Added `TestBelowPushFailure` class with 4 tests: raises ValueError, includes branch name, suggests connectivity check, lists created artifacts
  - `test/otstack/test_above.py` - Added `TestAbovePushFailure` class with 4 tests: same coverage as below
- **Learnings for future iterations:**
  - `TrackingCommandRunner.raise_called_process_error_for` matches any element in the command list (e.g., "push" matches `["git", "push", "-u", "origin", "branch"]`)
  - `subprocess.CalledProcessError` is already imported in `OtStackClient.py` via `import subprocess` at the top
  - The push step occurs after branch creation and worktree creation, so the error message can list both as already-created artifacts
---
